# Module 01: Fundamentals

This module covers the absolute basics of JavaScript. Before building complex apps, you need to understand how to store and manage data.

## 📝 Topics Covered

### 1. Variables (`variables.js`)
- **`let`**: For values that change.
- **`const`**: For values that stay the same (constants).
- **`var`**: The old way (avoid using this, but good to know!).

### 2. Data Types (`types.js`)
- **Primitives**: String, Number, Boolean, Null, Undefined.
- **Dynamic Typing**: How JavaScript figures out types automatically.

## 🏃 How to Run
Open your terminal and run:
```bash
node 01-fundamentals/variables.js
node 01-fundamentals/types.js
```
