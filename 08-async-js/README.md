# Module 08: Async JavaScript

JavaScript is single-threaded, but it handles long-running tasks (like fetching data) asynchronously to keep your app responsive.

## 📝 Topics Covered

### 1. Promises (`promises.js`)
- **Concept**: A value that may be available now, later, or never.
- **States**: Pending, Resolved, Rejected.

### 2. Async / Await (`async-await.js`)
- **Syntax**: Looking like synchronous code, but behaving asynchronously.
- **Try / Catch**: Handling errors cleanly.

### 3. Fetch API (`fetch-api.js`)
- **Network Requests**: Getting data from the web.

## 🏃 How to Run
```bash
node 08-async-js/promises.js
node 08-async-js/async-await.js
# fetch-api.js requires a browser or Node 18+
node 08-async-js/fetch-api.js
```
